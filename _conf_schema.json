{
  "enabled": {
    "type": "bool",
    "default": false,
    "description": "启用知识沉淀",
    "hint": "开启后，群聊、私聊和可用知识库范围留空表示全部启用；填写则仅使用指定范围。"
  },
  "platform_ids": {
    "type": "list",
    "default": [],
    "description": "平台实例范围",
    "hint": "填写 AstrBot 平台实例 ID，不是飞书 App ID。留空表示不额外限制平台实例。"
  },
  "group_ids": {
    "type": "list",
    "default": [],
    "description": "启用的群聊",
    "hint": "留空表示启用全部群聊；填写后仅启用指定群聊。填写飞书群 ID（oc_ 开头），每项一个。"
  },
  "private_ids": {
    "type": "list",
    "default": [],
    "description": "启用的私聊",
    "hint": "留空表示启用全部私聊；填写后仅启用指定用户的私聊。填写飞书用户 Open ID（ou_ 开头），每项一个。"
  },
  "writer_ids": {
    "type": "list",
    "default": [],
    "description": "允许写入的用户",
    "hint": "填写飞书用户 Open ID，每项一个。留空表示已启用会话内的全部成员均可写入。"
  },
  "template_kb": {
    "type": "text",
    "default": "",
    "description": "可用知识库",
    "hint": "留空表示所有知识库都可用；填写后仅使用指定库，名称或 ID 每行一个。Agent 根据当前话题选择项目和目标库，不会同时写入全部库；已有项目仍遵守共享权限。"
  },
  "project_shares_json": {
    "type": "text",
    "default": "{}",
    "description": "跨会话共享权限",
    "hint": "填写 JSON 对象：项目名对应允许访问的会话范围列表。会话范围通过 /知识管理状态 获取。此配置只控制访问权限，不决定话题所属项目；修改后需重新加载插件。"
  },
  "max_topic_chars": {
    "type": "int",
    "default": 100000,
    "description": "话题历史上限（字符）",
    "hint": "默认 100000 字符。超过上限会明确提示失败，不会静默截断话题后保存。"
  },
  "max_index_chunks": {
    "type": "int",
    "default": 5000,
    "description": "项目索引上限（块）",
    "hint": "默认每项目 5000 块。在上限内获取完整检索候选，过滤旧版本和其他平台的内容。超过上限需清理历史索引或调整此值。"
  },
  "review_timeout": {
    "type": "int",
    "default": 120,
    "description": "写入校验超时（秒）",
    "hint": "默认 120 秒。使用当前聊天模型校验保存意图和内容；超时不会确认保存成功。"
  }
}
