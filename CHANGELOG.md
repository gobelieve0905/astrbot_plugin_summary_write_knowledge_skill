# 更新记录

## Unreleased

- 将知识库模板改为可用知识库范围：留空全部可用，填写时按名称或 ID 限定；向 Agent 提供候选目录，支持选定已有库写入、项目同名库匹配和明确新项目建库。已有项目与权限保持绑定，拒绝不明确的多模型配置选择。

- 群聊和私聊列表改为留空全部启用，填写时仅启用指定范围；仍受总开关、平台实例范围和引用话题校验约束。写入用户列表留空仍允许全部已启用会话成员。

- 修复配置页长标题被省略的问题：使用简短标题，将完整说明移入 AstrBot 原生提示；配置键、类型和默认值保持不变。

- 新增独立的话题知识整理工具，支持背景知识、规则和项目 Skill。
- 接入引用续聊 v1 话题绑定，复用 AstrBot 4.28.0 知识入库、向量索引与现有重排序模型。
- 保存来源快照、平台、生效时间、版本；暂存后验证再生效，支持失败重试和版本冲突拒绝。
- 当前有效版本和平台过滤、会话共享权限、项目 Skill 按需读取。
- 增加纯本地测试及真实 AstrBot 隔离验证入口。

0.0.0 是初始开发占位版本，尚无候选、测试标签或正式发布。
